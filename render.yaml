services:
  - type: web
    name: stock-sync-mern
    env: node
    plan: free
    buildCommand: |
      npm install
      cd client
      npm install
      npm run build
    startCommand: node server/server.js
    envVars:
      - key: NODE_ENV
        value: production
